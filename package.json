{
  "name": "replay-proxy",
  "version": "1.0.0",
  "description": "A simple recording-replay proxy",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Jérôme WAX",
  "license": "ISC",
  "dependencies": {
    "http-proxy": "^1.16.2",
    "json-decycle": "^1.0.0"
  }
}
